# منصة التجارة الإلكترونية للتجار

منصة متكاملة باللغة العربية لإنشاء وإدارة المتاجر الإلكترونية، متوافقة مع Django backend.

## 📋 المحتويات

### الصفحات الرئيسية

1. **صفحة الهبوط** (`index.html`)
   - عرض مميزات المنصة
   - أزرار التسجيل وتسجيل الدخول
   - تصميم احترافي وجذاب

2. **تسجيل التاجر** (`register.html`)
   - نموذج تسجيل كامل مع التحقق
   - ربط مع API: `POST /api/auth/register`

3. **تسجيل الدخول** (`login.html`)
   - نموذج دخول بسيط
   - استعادة كلمة المرور عبر البريد
   - ربط مع API: `POST /api/auth/login`

4. **لوحة التاجر** (`dashboard.html`)
   - إحصائيات سريعة (طلبات اليوم، المبيعات، الطلبات المعلقة)
   - إدارة المنتجات (CRUD)
   - إدارة الطلبات مع أزرار تأكيد/إلغاء/تواصل
   - إعدادات الدفع (COD أو التواصل عبر وسائل التواصل)
   - لوحة الإشعارات
   - زر نسخ رابط المتجر

5. **صفحة المتجر العامة** (`store.html`)
   - تصميم white-label (بدون روابط للمنصة)
   - عرض المنتجات
   - نموذج طلب للعملاء
   - ربط مع API: `POST /api/store/{username}/orders`

6. **صفحة السياسات** (`policy.html`)
   - شروط الاستخدام
   - سياسة الخصوصية
   - صلاحيات الإدارة
   - آلية الشكاوى والاستئناف

7. **لوحة الإدارة** (`admin.html`)
   - إحصائيات عامة
   - إدارة التجار (عرض، تعليق، حذف)
   - إرسال إشعارات
   - سجل الأحداث (Audit Logs)
   - وضع Impersonate للعرض فقط

## 🎨 التصميم

### نظام الألوان
- **الأزرق الداكن**: `#0B3D91` (اللون الأساسي)
- **الرمادي الفاتح**: `#F4F6F9` (الخلفية)
- **الأبيض**: `#FFFFFF`

### الخطوط
- **Tajawal** من Google Fonts
- دعم كامل للغة العربية (RTL)

### المميزات
- تصميم responsive (محمول + ديسكتوب)
- أحجام خطوط واضحة (16px أساسي، H1=28px، H2=22px)
- أزرار كبيرة ملائمة للمس
- تباين ألوان عالي للقراءة

## 🔌 ربط API

### نقاط النهاية (Endpoints)

#### المصادقة
- `POST /api/auth/register` - تسجيل تاجر جديد
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/password-reset` - استعادة كلمة المرور

#### التاجر
- `GET /api/merchant/me` - بيانات التاجر
- `PUT /api/merchant/me` - تحديث بيانات التاجر
- `GET /api/merchant/orders` - طلبات التاجر
- `PUT /api/merchant/orders/{order_id}/status` - تحديث حالة الطلب
- `GET /api/merchant/products` - منتجات التاجر
- `POST /api/merchant/products` - إضافة منتج
- `PUT /api/merchant/products/{product_id}` - تحديث منتج
- `DELETE /api/merchant/products/{product_id}` - حذف منتج
- `POST /api/merchant/upload-image` - رفع صورة

#### المتجر العام
- `GET /api/store/{username}` - بيانات المتجر والمنتجات
- `POST /api/store/{username}/orders` - إنشاء طلب جديد

#### الإدارة
- `GET /api/admin/statistics` - إحصائيات عامة
- `GET /api/admin/merchants` - قائمة التجار
- `GET /api/admin/merchants/{merchant_id}` - تفاصيل تاجر
- `POST /api/admin/merchants/{merchant_id}/suspend` - تعليق تاجر
- `DELETE /api/admin/merchants/{merchant_id}` - حذف تاجر
- `POST /api/admin/notifications` - إرسال إشعار
- `GET /api/admin/audit-logs` - سجل الأحداث

### التحقق
- استخدام JWT tokens في localStorage
- إرسال `Authorization: Bearer <token>` في headers
- إرسال `X-CSRFToken` لطلبات Django
- التحقق عبر البريد الإلكتروني فقط (لا SMS)

## 📁 هيكل المشروع

```
5855/
├── index.html              # صفحة الهبوط
├── register.html           # تسجيل التاجر
├── login.html              # تسجيل الدخول
├── policy.html             # السياسات
├── dashboard.html          # لوحة التاجر
├── store.html              # المتجر العام
├── admin.html              # لوحة الإدارة
├── css/
│   ├── base.css           # نظام التصميم الأساسي
│   ├── landing.css        # أنماط صفحة الهبوط
│   ├── auth.css           # أنماط التسجيل/الدخول
│   ├── dashboard.css      # أنماط لوحة التاجر
│   ├── store.css          # أنماط المتجر العام
│   ├── admin.css          # أنماط لوحة الإدارة
│   └── policy.css         # أنماط صفحة السياسات
├── js/
│   ├── api.js             # طبقة الاتصال بـ API
│   ├── auth.js            # أدوات المصادقة
│   ├── utils.js           # دوال مساعدة
│   ├── dashboard.js       # منطق لوحة التاجر
│   ├── store.js           # منطق المتجر العام
│   └── admin.js           # منطق لوحة الإدارة
└── assets/
    └── images/            # الصور والأيقونات
```

## 🚀 الاستخدام

### 1. إعداد Backend URL
في ملف `js/api.js`، قم بتحديث:
```javascript
const API_BASE_URL = '/api'; // أو رابط Django backend الخاص بك
```

### 2. فتح الصفحات
- افتح `index.html` في المتصفح للبدء
- أو استخدم خادم محلي:
  ```bash
  python -m http.server 8000
  ```

### 3. روابط المتاجر
صيغة رابط المتجر:
```
yourdomain.com/store.html?username={username}
```

## ✨ المميزات الرئيسية

### لوحة التاجر
- ✅ إحصائيات فورية
- ✅ إدارة كاملة للمنتجات (إضافة، تعديل، حذف)
- ✅ إدارة الطلبات مع تحديث الحالة
- ✅ إعدادات الدفع المرنة
- ✅ لوحة الإشعارات
- ✅ نسخ رابط المتجر بنقرة واحدة

### المتجر العام
- ✅ تصميم white-label (بدون علامة المنصة)
- ✅ عرض احترافي للمنتجات
- ✅ نموذج طلب سهل للعملاء
- ✅ رابط صغير لصفحة السياسات فقط

### لوحة الإدارة
- ✅ إحصائيات شاملة
- ✅ إدارة التجار (عرض، تعليق، حذف)
- ✅ إرسال إشعارات مستهدفة
- ✅ سجل كامل للأحداث
- ✅ وضع Impersonate للعرض فقط

## 📱 التوافق

- ✅ Chrome, Firefox, Safari, Edge
- ✅ أجهزة محمولة (360x800)
- ✅ أجهزة سطح المكتب (1366x768+)
- ✅ دعم كامل لـ RTL

## 🔒 الأمان

- استخدام HTTPS في الإنتاج
- تخزين آمن للـ tokens
- التحقق من صحة المدخلات
- حماية CSRF
- رسائل خطأ واضحة بالعربية

## 📝 ملاحظات

1. **White-Label Store**: صفحة المتجر لا تحتوي على أي روابط للمنصة الرئيسية (فقط رابط السياسات في الفوتر)
2. **التحقق**: عبر البريد الإلكتروني فقط (لا SMS)
3. **الدفع**: خيارات COD أو التواصل عبر WhatsApp/Instagram/Facebook
4. **الإشعارات**: من الإدارة للتجار عبر لوحة التحكم

## 🎯 الخطوات التالية

1. ربط المشروع مع Django backend
2. اختبار جميع نقاط النهاية
3. إضافة صور وشعارات حقيقية
4. نشر المشروع على الخادم

## 📞 الدعم

للمساعدة أو الاستفسارات، تواصل عبر:
- البريد الإلكتروني: support@platform.com
- الهاتف: +213 555 123 456

---

**تم التطوير بـ ❤️ للتجار في الجزائر**
